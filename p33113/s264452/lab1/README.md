# OS laboratory work

> * Allocate memory with `mmap` and dump to files.
> * Work with `pthread`
> * System monitoring

### Run

```bash
make run
```

or compile sources:

```bash
gcc main.c -pthread
./a.out
```
### In strace we trust

Run script with strace:
```bash
./scripts/strace.sh
```



